name: zod-press
services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "3000:3000"
      - "9229:9229"
    volumes:
      - .:/opt/zod_press
    environment:
      - NODE_ENV=development
